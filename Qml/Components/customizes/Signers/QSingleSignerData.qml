import QtQuick 2.4

QtObject {
    property var single_name
    property var single_xpub
    property var single_publickey
    property var single_derivationPath
    property var single_masterFingerPrint
    property var single_masterSignerId
    property var single_lastHealthCheck
    property var single_signed_status
    property var single_type
    property var single_need_Topup_Xpub
    property var single_signed_message
    property var single_signed_signature
    property var single_signed_health
    property var single_isColdcard
    property var single_checked
    property var single_readyToSign
    property var single_is_local
    property var single_primary_key
    property var single_devicetype
    property var single_device_cardid
    property var single_tag
    property var single_has_sign_btn
    property var single_account_index
    property var single_isReplaced
    property var single_keyReplaced
    property var single_taproot_supported
    property var single_keyset_index
    property var single_keyset_status
    property var single_keyset_remaining
    property var single_value_key
    property var single_isOccupied
    property var single_needPassphrase
}
