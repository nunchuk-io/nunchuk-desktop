import QtQuick 2.4

QtObject {
    property var master_id
    property var master_name
    property var master_device
    property var master_checked
    property var master_fingerPrint
    property var master_messageToSign
    property var master_deviceType
    property var master_type
    property var master_need_passphrase
    property var master_need_pin
    property var master_need_xpub
    property var master_primary_key
    property var master_tag
    property var master_taproot_supported
    property var master_needBackup
}
